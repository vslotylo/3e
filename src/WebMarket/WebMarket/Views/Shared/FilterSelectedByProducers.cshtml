@model WebMarket.Filters.ProducersFilter
@for (int i = 0; i < Model.ProducersList.Count; i++)
{
    <a class="close" href="@Url.Action("index", Model.Routes[i])">&times;</a>
    <span>@Model.DisplayList[i]</span>
    <br />            
}

